etoolbox.utils.pudl
===================

.. py:module:: etoolbox.utils.pudl

.. autoapi-nested-parse::

   Functions and objects for accessing PUDL data.



Classes
-------

.. autoapisummary::

   etoolbox.utils.pudl.ContentHashFileSystem
   etoolbox.utils.pudl.PretendPudlTablCore


Functions
---------

.. autoapisummary::

   etoolbox.utils.pudl.rmi_pudl_init
   etoolbox.utils.pudl.rmi_pudl_clean
   etoolbox.utils.pudl.pudl_list
   etoolbox.utils.pudl.pd_read_pudl
   etoolbox.utils.pudl.pl_scan_pudl
   etoolbox.utils.pudl.pl_read_pudl
   etoolbox.utils.pudl.conform_pudl_dtypes
   etoolbox.utils.pudl.get_pudl_sql_url


Module Contents
---------------

.. py:function:: rmi_pudl_init(b64_encoded = None)

   Setup rmi.pudl to provide access to PUDL tables from GCS with local caching.

   :param b64_encoded: base64 encoding of the service_account json for PUDL GCS access.

   Returns: a boolean indicating if the token was written to disk.



.. py:function:: rmi_pudl_clean(*, delete_config = False)

   Remove rmi.pudl local cache.


.. py:class:: ContentHashFileSystem(project=DEFAULT_PROJECT, access='full_control', token=None, block_size=None, consistency='none', cache_timeout=None, secure_serialize=True, check_connection=None, requests_timeout=None, requester_pays=False, asynchronous=False, session_kwargs=None, loop=None, timeout=None, endpoint_url=None, default_location=None, version_aware=False, **kwargs)

   Bases: :py:obj:`gcsfs.GCSFileSystem`


   GCS filesystem where cache validity is based on content hash only.

   Create and configure file-system instance

   Instances may be cachable, so if similar enough arguments are seen
   a new instance is not required. The token attribute exists to allow
   implementations to cache instances if they wish.

   A reasonable default should be provided if there are no arguments.

   Subclasses should call this method.

   :param use_listings_cache: passed to ``DirCache``, if the implementation supports
                              directory listing caching. Pass use_listings_cache=False
                              to disable such caching.
   :param listings_expiry_time: passed to ``DirCache``, if the implementation supports
                                directory listing caching. Pass use_listings_cache=False
                                to disable such caching.
   :param max_paths: passed to ``DirCache``, if the implementation supports
                     directory listing caching. Pass use_listings_cache=False
                     to disable such caching.
   :param skip_instance_cache: If this is a cachable implementation, pass True here to force
                               creating a new instance even if a matching instance exists, and prevent
                               storing this instance.
   :type skip_instance_cache: bool
   :param asynchronous:
   :type asynchronous: bool
   :param loop:
   :type loop: asyncio-compatible IOLoop or None


   .. py:method:: ukey(path)

      Hash of file content, to tell if it has changed.



.. py:function:: pudl_list(release = 'nightly', token = None, *, detail = False)

   List PUDL tables in GCS using ``ls`` command.

   :param release: ``nightly``, ``stable`` or versioned, pass ``None`` to list all
   :param token: token or path to token for PUDL GCS access
   :param detail: if True, return details of each table, otherwise just names


.. py:function:: pd_read_pudl(table_name, release = 'nightly', token = None, **kwargs)

   Read PUDL table from GCS as :class:`pandas.DataFrame`.

   :param table_name: name of table in PUDL sqlite database
   :param release: ``nightly`` or ``stable``
   :param token: token or path to token for PUDL GCS access
   :param kwargs: passed to :func:`pandas.read_parquet`


.. py:function:: pl_scan_pudl(table_name, release = 'nightly', token = None, *, use_polars=False, **kwargs)

   Read PUDL table from GCS as :class:`polars.LazyFrame`.

   .. note::

      Accessing PUDL tables directly from GCS using polars requires version 0.20
      or higher.

   :param table_name: name of table in PUDL sqlite database
   :param release: ``nightly`` or ``stable``
   :param token: path to token for PUDL GCS access (can be a dict if use_polars=False)
   :param use_polars: If ``True``, use polars GCP client, this does not
                      work with local caching. If ``False``, use
                      :class:`fsspec.implementations.cached.WholeFileCacheFileSystem`
                      for file access and caching.
   :param kwargs: passed to :func:`polars.scan_parquet`


.. py:function:: pl_read_pudl(table_name, release = 'nightly', token = None, *, use_polars=False, **kwargs)

   Read PUDL table from GCS as :class:`polars.DataFrame`.

   .. note::

      Accessing PUDL tables directly from GCS using polars requires version 0.20
      or higher.

   :param table_name: name of table in PUDL sqlite database
   :param release: ``nightly`` or ``stable``
   :param token: path to token for PUDL GCS access
   :param use_polars: use polars GCP client rather than GCSFs, this does not
                      work with local caching
   :param kwargs: passed to :func:`polars.scan_parquet`


.. py:function:: conform_pudl_dtypes(df)

   Conform types of PUDL columns to those in PudlTabl.

   :param df: a dataframe with columns from PUDL

   Returns: the pudl table with standardized dtypes

   .. rubric:: Examples

   .. code-block:: python

       import pandas as pd
       import sqlalchemy as sa

       from etoolbox.utils.pudl import get_pudl_sql_url, conform_pudl_dtypes

       pd.read_sql_table(table_name, sa.create_engine(get_pudl_sql_url())).pipe(
           conform_pudl_dtypes
       )


.. py:function:: get_pudl_sql_url(file=PUDL_CONFIG)

   Get the URL for the pudl.sqlite DB.


.. py:class:: PretendPudlTablCore

   A DataZip of a PudlTabl can be recreated with this to avoid importing PUDL.

   .. admonition:: DeprecationWarning
      :class: warning

      ``PretendPudlTablCore`` will be removed in a future version, read tables directly
      from the sqlite.



