:py:mod:`etoolbox.datazip.core`
===============================

.. py:module:: etoolbox.datazip.core

.. autoapi-nested-parse::

   Code for :class:`.DataZip`.



Module Contents
---------------

Classes
~~~~~~~

.. autoapisummary::

   etoolbox.datazip.core.DataZip



Functions
~~~~~~~~~

.. autoapisummary::

   etoolbox.datazip.core.obj_from_recipe



Attributes
~~~~~~~~~~

.. autoapisummary::

   etoolbox.datazip.core.LOGGER
   etoolbox.datazip.core.RECIPES


.. py:data:: LOGGER
   

   

.. py:data:: RECIPES
   :annotation: :dict[tuple, dict]

   

.. py:function:: obj_from_recipe(thing, module, klass = None, constructor = None)

   Recreate an object from a recipe.

   Can be module.klass(thing), module.constructor(thing), or
   module.klass.constructor(thing).

   :param thing: what will be passed to ``constructor`` / ``__init__``
   :param module: the module that the function or class can be found in
   :param klass: The __qualname__ of the class, if needed. If None, then
                 ``constructor`` must be a function.
   :param constructor: A constructor method on a class if ``klass`` is specified,
                       otherwise a function that returns the desired object. If None, the
                       class ``klass`` will be created using its ``__init__``.


.. py:class:: DataZip(file, mode='r', recipes=None, *args, **kwargs)

   Bases: :py:obj:`zipfile.ZipFile`

   SubClass of :class:`ZipFile` with methods for easier use with :mod:`utils`.

   z = DataZip(file, mode="r", compression=ZIP_STORED, allowZip64=True,
               compresslevel=None)


   Open the ZIP file.

   :param file: Either the path to the file, or a file-like object.
                If it is a path, the file will be opened and closed by ZipFile.
   :param mode: The mode can be either read 'r', write 'w', exclusive create 'x',
                or append 'a'.
   :param recipes: add more customization on how attributes will be stored
                   organized like :py:const:`etoolbox.datazip.core.RECIPES`
   :param compression: ZIP_STORED (no compression), ZIP_DEFLATED (requires zlib),
                       ZIP_BZIP2 (requires bz2) or ZIP_LZMA (requires lzma).
   :param allowZip64: if True ZipFile will create files with ZIP64 extensions when
                      needed, otherwise it will raise an exception when this would
                      be necessary.
   :param compresslevel: None (default for the given compression type) or an integer
                         specifying the level to pass to the compressor.
                         When using ZIP_STORED or ZIP_LZMA this keyword has no effect.
                         When using ZIP_DEFLATED integers 0 through 9 are accepted.
                         When using ZIP_BZIP2 integers 1 through 9 are accepted.

   .. py:property:: recipes
      :type: dict[tuple, dict]

      Combination of internal and constant recipes.

   .. py:method:: read(name, pwd = ..., super_=False)

      Return obj or bytes for name.


   .. py:method:: read_dfs()

      Read all dfs lazily.


   .. py:method:: readm(key=None)

      Read metadata.


   .. py:method:: writed(name, data, **kwargs)

      Write dict, df, str, or some other objects to name.


   .. py:method:: writem(key, data)

      Write metadata.


   .. py:method:: close()

      Close the file, and for mode 'w' write attributes and metadata.


   .. py:method:: dfs_to_zip(path, df_dict, clobber=False)
      :classmethod:

      Create a zip of parquets.

      :param df_dict: dict of dfs to put into a zip
      :param path: path for the zip
      :param clobber: if True, overwrite exiting file with same path

      Returns: None



   .. py:method:: dfs_from_zip(path)
      :classmethod:

      Dict of dfs from a zip of parquets.

      :param path: path of the zip to load

      Returns: dict of dfs



   .. py:method:: _json_get(*args)


   .. py:method:: _stemlist()


   .. py:method:: _recursive_read(stem)


   .. py:method:: _read_df(name)


   .. py:method:: _recursive_write(name, data)


   .. py:method:: _write_numpy(name, data, **kwargs)


   .. py:method:: _write_jsonable(name, obj, **kwargs)

      Write a dict in the ZIP as json.


   .. py:method:: _write_using_recipe(name, data, obj_info)


   .. py:method:: _write_as_str(name, obj, **kwargs)

      Write an object as whatever str is in the parentheses of its repr.


   .. py:method:: _write_obj(name, obj, **kwargs)


   .. py:method:: _write_df(name, df, **kwargs)

      Write a df in the ZIP as parquet.


   .. py:method:: _write_image(name, data, **kwargs)


   .. py:method:: _str_cols(df, *args)
      :staticmethod:


   .. py:method:: _objinfo(obj, constructor=None)
      :staticmethod:



